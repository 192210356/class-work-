#include <stdio.h>

// Function to find the square of a number
int square(int num) {
    return num * num;
}

int main() {
    int num, sq;
    
    printf("Enter a number: ");
    scanf("%d", &num);
    
    sq = square(num);
    printf("The square of %d is %d", num, sq);
    
    return 0;
}
